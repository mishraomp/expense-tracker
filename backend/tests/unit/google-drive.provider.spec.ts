import { describe, it, expect } from 'vitest';

// Deprecated placeholder tests removed after migrating to per-user OAuth provider.
// Keeping a trivial test to satisfy Vitest suite without relying on Google APIs.
describe('GoogleDriveProvider (migrated)', () => {
  it('migration placeholder', () => {
    expect(true).toBe(true);
  });
});
