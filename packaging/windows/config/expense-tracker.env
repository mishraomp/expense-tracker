# Expense Tracker API Configuration
# Environment variables for the ExpenseTrackerAPI service

# Database connection
DATABASE_URL=postgresql://expense_tracker:expense_tracker_db@localhost:5432/expense_tracker

# API settings
PORT=3000
NODE_ENV=production

# CORS origin (frontend URL)
CORS_ORIGIN=http://localhost

# JWT settings
# IMPORTANT: Generate a secure secret for production!
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING
JWT_EXPIRES_IN=7d

# Keycloak settings
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=expense-tracker
KEYCLOAK_CLIENT_ID=expense-tracker-api
